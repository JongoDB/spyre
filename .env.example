# Proxmox API token secret (stored separately from environment.yaml for security)
SPYRE_PVE_TOKEN_SECRET=your-token-secret-here

# Base URL for OAuth callbacks
SPYRE_BASE_URL=https://spyre.local

# Node environment
NODE_ENV=development
